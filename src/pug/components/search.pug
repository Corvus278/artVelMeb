mixin search(placeholder = 'Поиск')
	.search
		form.search__form
			input.search__input(type="text" name="search" placeholder=placeholder)
			button.search__btn.btn.btn-success.btn-oval(type="submit")
				img.search__btn-icon(src="./assets/icons/search.svg")
